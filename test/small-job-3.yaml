apiVersion: v1
kind: Pod
metadata:
  name: small-job-3
  labels:
    job-size: small
spec:
  containers:
    - name: workload
      image: busybox
      command: ["sleep", "3600"]
      resources:
        requests:
          simulated.com/gpu: "2"
        limits:
          simulated.com/gpu: "2"